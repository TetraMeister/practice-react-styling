import styled, { css } from "styled-components";

const StyledCardTitle = styled.div`
  margin-bottom: .75rem;
  font-size: 1.25rem;
  font-weight: 500;
  line-height: 1.2;
`

export {StyledCardTitle}