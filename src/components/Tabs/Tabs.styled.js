import styled, {css} from "styled-components";

const StyledTabs = styled.nav`
    display: flex;
    flex-wrap: wrap;
    padding-left: 0;
    margin-bottom: 0;
    border-bottom: 1px solid #dee2e6;
`

export {StyledTabs}